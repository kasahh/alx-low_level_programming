#include <stdio.h>
/**
 * main - prints to std out using printf
 *
 * Return: 0 if success
 */
int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);
	return (0);
}
